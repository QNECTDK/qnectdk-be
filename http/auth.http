### 회원가입
POST {{baseUrl}}/api/auth/signup
Content-Type: application/json

{
  "loginId": "{{loginId}}",
  "phone": "{{phone}}",
  "password": "{{password}}",
  "name": "테스트",
  "birthDate": "2003-05-01"
}

### 아이디 중복 확인 (available: false 면 이미 사용 중)
GET {{baseUrl}}/api/auth/check-login-id?loginId={{loginId}}

### 로그인 → access/refresh 토큰 캡처
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "loginId": "{{loginId}}",
  "password": "{{password}}"
}

> {%
client.global.set("accessToken", response.body.data.accessToken);
client.global.set("refreshToken", response.body.data.refreshToken);
%}

### 토큰 재발급 (refresh 회전 — 이후 새 토큰으로 갱신됨)
POST {{baseUrl}}/api/auth/reissue
Content-Type: application/json

{
  "refreshToken": "{{refreshToken}}"
}

> {%
client.global.set("accessToken", response.body.data.accessToken);
client.global.set("refreshToken", response.body.data.refreshToken);
%}

### 로그아웃 (refresh 토큰 폐기)
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{
  "refreshToken": "{{refreshToken}}"
}

### (검증) 잘못된 입력 → 400 + 필드별 에러
POST {{baseUrl}}/api/auth/signup
Content-Type: application/json

{
  "loginId": "1A",
  "phone": "123",
  "password": "short",
  "name": "",
  "birthDate": "2999-01-01"
}

### (검증) 없는 아이디/틀린 비번 → 401 INVALID_CREDENTIALS (동일 메시지)
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "loginId": "nouser01",
  "password": "wrongpass1"
}
